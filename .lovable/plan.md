
The user is moving to VSCode and wants a clean handoff: what env vars are currently wired up and what's still needed. This is a research/inventory question — no code changes. Let me pull exact values from `.env.example`, `firebase.ts`, and `mapsLoader.ts`.
